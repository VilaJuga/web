rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Dev rules: open for emulator testing. TIGHTEN before deploying to production.
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
